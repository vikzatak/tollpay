@model IEnumerable<DPTPWebAPI.TranP_MaterialTypes>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MaterialType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MaterialStatus)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MaterialType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MaterialStatus)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.srno }) |
            @Html.ActionLink("Details", "Details", new { id=item.srno }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.srno })
        </td>
    </tr>
}

</table>
